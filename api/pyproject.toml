[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/common",
    "apps/rest-api",
    "packages/typeforms-domain-dataaccess",
    "packages/typeforms-domain",
]
